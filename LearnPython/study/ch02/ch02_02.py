#!/usr/bin/env python
# -*- coding: utf8 -*-

# 元组 不可变序列
# 如果元组只有一个元素，必须在最后一个位置上加上逗号
# tuple() 功能与 list() 函数基本上一样

# 元组存在的意义
# 1、元组可以在映射中当做健使用，而列表不行
# 2、元组作为很多内建函数和方法的返回值存在，也就是说你必须对元组
# 进行处理，只要不尝试修改元组，那么，"处理" 元组在绝大多数情况下
# 就可以把它们当做列表来进行处理

# 一般来说，列表肯能更能满足对序列的所有需求
